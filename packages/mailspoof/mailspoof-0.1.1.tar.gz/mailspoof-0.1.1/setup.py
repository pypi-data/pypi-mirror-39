#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mailspoof']

package_data = \
{'': ['*']}

install_requires = \
['dnspython == 1.15.0', 'tldextract == 2.2.0', 'requests == 2.20.1']

entry_points = \
{'console_scripts': ['mailspoof = mailspoof:main']}

setup(name='mailspoof',
      version='0.1.1',
      description='Scans SPF and DMARC records for issues that could allow email spoofing.',
      author='Alex Kaskasoli',
      author_email='alexkaskasoli@hotmail.com',
      url='https://alex.kaskaso.li/',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
     )
