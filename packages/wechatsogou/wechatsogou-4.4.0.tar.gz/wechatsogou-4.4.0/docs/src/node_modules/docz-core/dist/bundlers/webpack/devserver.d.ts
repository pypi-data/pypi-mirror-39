import { Compiler, Configuration } from 'webpack';
import { Config } from '../../commands/args';
export declare const devServerConfig: (args: Config, compiler: Compiler, config: Configuration) => {
    compiler: Compiler;
    host: string;
    port: number;
    content: string[];
    logLevel: string;
    dev: {
        logLevel: string;
    };
    hot: {
        reload: boolean;
        logLevel: string;
    };
    add: (app: any) => void;
};
