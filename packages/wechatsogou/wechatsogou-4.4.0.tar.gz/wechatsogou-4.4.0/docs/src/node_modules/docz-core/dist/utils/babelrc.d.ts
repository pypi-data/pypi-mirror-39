import { Config } from '../commands/args';
export interface BabelRC {
    presets?: any[];
    plugins?: any[];
    cacheDirectory?: boolean;
    babelrc?: boolean;
}
export declare const babelrc: (args: Config) => BabelRC;
