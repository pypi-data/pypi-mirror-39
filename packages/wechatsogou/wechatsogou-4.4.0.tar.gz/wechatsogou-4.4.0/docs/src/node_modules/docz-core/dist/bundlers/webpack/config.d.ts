import Config from 'webpack-chain';
import { Config as Args } from '../../commands/args';
import { BabelRC } from '../../utils/babelrc';
import { Env } from './';
export declare const createConfig: (babelrc: BabelRC) => (args: Args, env: Env) => Config;
