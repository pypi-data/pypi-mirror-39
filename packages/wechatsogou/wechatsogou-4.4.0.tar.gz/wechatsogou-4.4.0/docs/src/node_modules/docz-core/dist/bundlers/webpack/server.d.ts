import webpack from 'webpack';
import { BundlerServer } from '../../Bundler';
import { Config as Args } from '../../commands/args';
export declare const server: (args: Args) => (config: webpack.Configuration) => Promise<BundlerServer>;
