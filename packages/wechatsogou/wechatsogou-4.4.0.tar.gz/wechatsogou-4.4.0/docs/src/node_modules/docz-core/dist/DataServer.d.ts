import { Config } from './commands/args';
import { Entries } from './Entries';
export interface DataServerOpts {
    server: any;
    config: Config;
}
export declare class DataServer {
    private server;
    private config;
    constructor({ server, config }: DataServerOpts);
    processEntries(entries: Entries): Promise<void>;
    processThemeConfig(): Promise<void>;
    private dataObj;
    private entriesData;
    private configData;
    private updateEntries;
    private updateConfig;
}
